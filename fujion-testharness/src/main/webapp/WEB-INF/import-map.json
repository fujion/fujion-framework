/* Example of a global import map.
*  Note that for webjar-embedded resources, the "./webjars/name/" prefix is required, where
*  "name" is the name of the webjar.
*/

{
  "imports": {
      "test-import": "./webjars/name/path/xxx.js"
  }
}
